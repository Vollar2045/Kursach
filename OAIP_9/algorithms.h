#ifndef ALGORITHMS_H
#define ALGORITHMS_H

#include "stack.h"
#include <string>
using namespace std;

void sort(Stack*& top);
void filterPatients(Stack*& top);
void searchPatient(Stack*& top);
void extra_task(Stack*& top);

#endif
